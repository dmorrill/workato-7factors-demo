@extends('layouts.app')

@section('title', '{{ $campaign->hero_headline }} — Workato')

@section('content')

{{-- Hero --}}
<section class="bg-hero-dark text-white py-24 px-6 lg:px-8">
    <div class="max-w-5xl mx-auto text-center">
        <div class="inline-flex items-center gap-2 bg-[#0C3A70] border border-gray-700 rounded-full px-4 py-2 text-sm text-gray-300 mb-8">
            <span class="w-2 h-2 bg-[#67EADD] rounded-full"></span>
            {{ $campaign->framework_name }} · Workato
        </div>
        <h1 class="text-4xl md:text-6xl font-bold font-display leading-tight tracking-tight mb-6">
            {{ $campaign->hero_headline }}
        </h1>
        <p class="text-xl text-gray-300 max-w-3xl mx-auto leading-relaxed mb-10">
            {{ $campaign->hero_subheadline }}
        </p>
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a href="#concepts" class="bg-[#67EADD] hover:bg-[#21DBC8] text-[#083763] font-semibold px-8 py-3.5 rounded-lg transition-colors text-base">
                Explore the framework
            </a>
            <a href="{{ route('generated.campaign', $campaign->slug) }}"
               class="bg-[#0C3A70] border border-gray-600 hover:border-gray-400 hover:bg-[#0D4080] text-white font-semibold px-8 py-3.5 rounded-lg transition-colors text-base">
                See the campaign brief
            </a>
        </div>
    </div>
</section>

{{-- Video (if YouTube source) --}}
@if($campaign->video_url)
<section class="bg-[#083763] pb-20 px-6 lg:px-8">
    <div class="max-w-4xl mx-auto">
        <div class="relative rounded-2xl overflow-hidden shadow-2xl border border-gray-700" style="padding-bottom: 56.25%; height: 0;">
            <iframe class="absolute top-0 left-0 w-full h-full"
                    src="{{ $campaign->video_url }}"
                    title="{{ $campaign->title }}"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen></iframe>
        </div>
    </div>
</section>
@endif

{{-- Framework concepts --}}
<section id="concepts" class="bg-hero-dark border-t-4 border-[#67EADD] py-24 px-6 lg:px-8">
    <div class="max-w-6xl mx-auto">
        <div class="text-center mb-16">
            <h2 class="text-3xl md:text-4xl font-bold font-display text-white mb-4">{{ $campaign->framework_name }}</h2>
            <p class="text-lg text-gray-400 max-w-2xl mx-auto">{{ $campaign->framework_intro }}</p>
        </div>

        <div class="divide-y divide-[#1A4A8A] border border-[#1A4A8A] rounded-2xl overflow-hidden">
            @foreach($campaign->concepts as $concept)
            <div class="flex items-center gap-6 px-7 py-5 bg-[#0C3A70]">
                <span class="text-2xl font-bold text-[#67EADD]/50 w-8 shrink-0 font-display">{{ $concept->number }}</span>
                <div class="flex-1 min-w-0">
                    <p class="text-white font-semibold text-sm">{{ $concept->name }}</p>
                    <p class="text-gray-300 text-sm mt-0.5">{{ $concept->principle }}</p>
                </div>
            </div>
            @endforeach
        </div>
    </div>
</section>

{{-- Context section --}}
@if($campaign->context_headline)
<section class="bg-[#F4F2E3] py-24 px-6 lg:px-8">
    <div class="max-w-4xl mx-auto text-center">
        <p class="text-xs font-semibold text-[#1D60CA] uppercase tracking-wider mb-4">Why it matters</p>
        <h2 class="text-3xl md:text-4xl font-bold font-display text-gray-900 mb-6 leading-tight">{{ $campaign->context_headline }}</h2>
        <p class="text-lg text-gray-500 max-w-2xl mx-auto leading-relaxed">{{ $campaign->context_body }}</p>
    </div>
</section>
@endif

{{-- CTA --}}
<section class="bg-[#083763] py-20 px-6 lg:px-8">
    <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold font-display text-white mb-4">{{ $campaign->cta_headline ?? 'This is an industry conversation.' }}</h2>
        <p class="text-gray-400 text-lg leading-relaxed mb-10">
            {{ $campaign->cta_body ?? 'Join us.' }}
        </p>
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a href="{{ route('generated.campaign', $campaign->slug) }}"
               class="bg-[#67EADD] hover:bg-[#21DBC8] text-[#083763] font-semibold px-8 py-3.5 rounded-lg transition-colors">
                See the campaign brief
            </a>
            <a href="{{ route('make.index') }}"
               class="border border-gray-600 hover:border-gray-400 text-white font-semibold px-8 py-3.5 rounded-lg transition-colors">
                Back to Campaign Maker
            </a>
        </div>
        <p class="text-gray-600 text-sm mt-8">Generated by Workato Campaign Maker · <a href="{{ $campaign->source_url }}" target="_blank" class="text-gray-400 hover:text-white transition-colors">Source →</a></p>
    </div>
</section>

@endsection
